#include <bits/stdc++.h>
using namespace std;

#define MAX 10000

typedef int array[MAX];
int j = 0;

int main()
{
    int n_cases, books, scribers;

    scanf("%d", &n_cases);

    for (int i = 0; i < n_cases; ++i)
    {
        scanf("%d %d", &books, %scribers);

        array pages;

        for (j = 0; j < books; ++j)
        {
            scanf("%d", &pages[j]);
        }
    }
    return 0;
}
